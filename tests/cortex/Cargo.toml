[package]
name = "cortex-m-linkme-test"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
cortex-m = "0.7"
cortex-m-rt = "0.7"
cortex-m-semihosting = "0.5"
linkme = { path = "../.." }
panic-semihosting = { version = "0.6", features = ["exit"] }
